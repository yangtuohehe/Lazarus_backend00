<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.lazarus_backend00.dao.FeatureDao">

    <insert id="insert"
            parameterType="com.example.lazarus_backend00.infrastructure.persistence.entity.FeatureEntity"
            useGeneratedKeys="true"
            keyProperty="id">

        INSERT INTO feature (
            feature_name,
            created_at,
            updated_at
        )
        VALUES (
                   #{featureName},
                   #{createdAt},
                   #{updatedAt}
               )
    </insert>

    <select id="selectById"
            resultType="com.example.lazarus_backend00.infrastructure.persistence.entity.FeatureEntity">

        SELECT
            id,              feature_name    AS featureName,
            created_at      AS createdAt,
            updated_at      AS updatedAt
        FROM feature
        WHERE id = #{id}     </select>

    <select id="selectByCondition"
            parameterType="com.example.lazarus_backend00.infrastructure.persistence.entity.FeatureEntity"
            resultType="com.example.lazarus_backend00.infrastructure.persistence.entity.FeatureEntity">

        SELECT
        id,
        feature_name    AS featureName,
        created_at      AS createdAt,
        updated_at      AS updatedAt
        FROM feature
        <where>
            <if test="id != null">
                AND id = #{id}
            </if>

            <if test="featureName != null and featureName != ''">
                AND feature_name = #{featureName}
            </if>

            <if test="createdAt != null">
                AND created_at = #{createdAt}
            </if>
        </where>

        ORDER BY created_at DESC
    </select>

</mapper>